# Dependencies (there are none today, but just in case)
node_modules/

# Your private review decisions and notes. Never publish these.
data/decisions.json
data/*.tmp

# Logs and OS clutter
*.log
.DS_Store

iammoody-export

# NOTE: dist/ is committed ON PURPOSE. It is the built website that gets published.
# (The build needs ../iammoody-export, which is not part of this repository, so GitHub cannot build it for you.)

# Optimized images (regenerated by the build; safe to delete)
.cache/
